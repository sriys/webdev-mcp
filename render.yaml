services:
  - type: web
    name: webdev-mcp
    runtime: python
    buildCommand: pip install -r requirements.txt
    startCommand: python server.py --http
    envVars:
      - key: PYTHON_VERSION
        value: 3.12.0
